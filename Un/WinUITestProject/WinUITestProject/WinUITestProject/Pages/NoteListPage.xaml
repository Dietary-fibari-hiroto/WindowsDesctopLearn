<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUITestProject.Pages.NoteListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUITestProject.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
            Text="📝 メモ一覧"
            Style="{StaticResource TitleTextBlockStyle}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Click="Refresh_Click" ToolTipService.ToolTip="更新">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                </Button>
                <Button
                Style="{StaticResource AccentButtonStyle}"
                Click="NewNote_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                        <TextBlock Text="新規メモ" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- 検索バー -->
        <AutoSuggestBox
        x:Name="SearchBox"
        Grid.Row="1"
        PlaceholderText="メモを検索..."
        QueryIcon="Find"
        Margin="0,0,0,16"
        TextChanged="SearchBox_TextChanged" />

        <!-- ノートカード一覧 -->
        <ScrollViewer Grid.Row="2">
            <ItemsRepeater x:Name="NoteRepeater" />
        </ScrollViewer>

        <!-- ローディング -->
        <ProgressBar
        x:Name="LoadingBar"
        Grid.Row="2"
        IsIndeterminate="True"
        Visibility="Collapsed"
        VerticalAlignment="Top" />

        <!-- 空状態 -->
        <StackPanel
        x:Name="EmptyState"
        Grid.Row="2"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="12"
        Visibility="Collapsed">
            <FontIcon Glyph="&#xE70B;" FontSize="48"
                  Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
            <TextBlock
            Text="メモがありません"
            Style="{StaticResource SubtitleTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            HorizontalAlignment="Center" />
            <Button
            Content="最初のメモを作成"
            Style="{StaticResource AccentButtonStyle}"
            Click="NewNote_Click"
            HorizontalAlignment="Center" />
        </StackPanel>

        <!-- ステータスバー -->
        <Border
        Grid.Row="3"
        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
        Padding="12,8" CornerRadius="4" Margin="0,12,0,0">
            <TextBlock
            x:Name="StatusTextBlock"
            Text=""
            Style="{StaticResource CaptionTextBlockStyle}" />
        </Border>
    </Grid>
</Page>
